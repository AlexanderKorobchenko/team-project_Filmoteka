<form id="form-for-genre">
    <label for="genre">Search by
        <select name="genre" id="genre">
            <option selected="selected">genres</option>
            {{#each this}}
            <option value="{{id}}">{{name}}</option>
            {{/each}}
    </label>
</form>